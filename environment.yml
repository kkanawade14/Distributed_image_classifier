name: multi-gpu-cifar10
dependencies:
  - python=3.10
  - pip
  - pytorch>=2.0
  - torchvision>=0.15
  - cudatoolkit
  - pip:
      - timm>=0.9.0
      - tensorboard>=2.13
      - pyyaml>=6.0
      - tqdm>=4.65
      - pandas>=1.5
      - matplotlib>=3.7
channels:
  - pytorch
  - conda-forge
  - defaults
